# Arduino IDE 烧录 ESP32-S3 配置截图说明

## 📸 Arduino IDE 配置界面说明

由于无法直接提供截图，这里用文字详细描述每个配置项的位置和设置方法。

---

## 1️⃣ 首选项配置（添加 ESP32 板卡包）

**操作路径：** 文件 → 首选项

**需要设置的项：**

```
┌─────────────────────────────────────────────────────────┐
│  首选项                                           [×]   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Sketchbook 位置:                                       │
│  [C:\Users\...\Arduino                        ] [浏览]  │
│                                                         │
│  编辑器语言: [简体中文                        ▼]        │
│                                                         │
│  编辑器字体大小: [12                          ▼]        │
│                                                         │
│  主题: [默认                                  ▼]        │
│                                                         │
│  附加开发板管理器网址:                                  │
│  ┌─────────────────────────────────────────────────┐   │
│  │https://espressif.github.io/arduino-esp32/...   │   │  ← 在这里添加
│  │                                                 │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [确定]  [取消]                                         │
└─────────────────────────────────────────────────────────┘
```

**要添加的网址：**
```
https://espressif.github.io/arduino-esp32/package_esp32_index.json
```

或国内镜像：
```
https://mirrors.tuna.tsinghua.edu.cn/espressif/arduino-esp32/package_esp32_index.json
```

---

## 2️⃣ 开发板管理器（安装 ESP32）

**操作路径：** 工具 → 开发板 → 开发板管理器

**界面说明：**

```
┌─────────────────────────────────────────────────────────┐
│  开发板管理器                                     [×]   │
├─────────────────────────────────────────────────────────┤
│  [搜索框: esp32                                    ]    │  ← 搜索 "esp32"
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  esp32  by Espressif Systems               INSTALLED│  ← 找到这个
│  │  ★★★★★                              版本 [2.0.14▼] │
│  │  ESP32 Arduino Core                               │
│  │                                                   │
│  │  [移除]  [更新]                                   │  ← 点击"安装"或"更新"
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  其他开发板...                                          │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**注意：**
- 推荐版本：2.0.14 或 3.0.x（最新稳定版）
- 安装需要几分钟，耐心等待

---

## 3️⃣ 选择开发板

**操作路径：** 工具 → 开发板 → ESP32 Arduino → ESP32S3 Dev Module

**菜单层级：**

```
工具
 └─ 开发板 >
     ├─ Arduino AVR Boards >
     ├─ ESP32 Arduino >                              ← 展开这个
     │   ├─ ESP32 Dev Module
     │   ├─ ESP32-S2 Dev Module
     │   ├─ ESP32S3 Dev Module                       ← 选择这个 ✓
     │   ├─ ESP32-C3 Dev Module
     │   └─ ...
     └─ 开发板管理器...
```

---

## 4️⃣ 工具菜单完整配置

**操作路径：** 工具菜单

选择开发板后，工具菜单会出现很多新选项，逐一配置：

```
工具
 ├─ 开发板: "ESP32S3 Dev Module"              ← 已选择
 ├─ Port: "COM3"                                ← 根据实际选择
 ├─ Upload Speed: "921600"                      ← 可改为 115200
 ├─ USB Mode: "Hardware CDC and JTAG"           ← 必须
 ├─ USB CDC On Boot: "Enabled"                  ← 必须启用
 ├─ USB Firmware MSC On Boot: "Disabled"
 ├─ USB DFU On Boot: "Disabled"
 ├─ Upload Mode: "UART0 / Hardware CDC"
 ├─ CPU Frequency: "240MHz (WiFi)"              ← 推荐
 ├─ Flash Mode: "QIO 80MHz"                     ← 默认
 ├─ Flash Size: "4MB (32Mb)"                    ← 根据实际选择
 ├─ Partition Scheme: "Default 4MB with spiffs" ← 推荐
 ├─ Core Debug Level: "无"                      ← 可选 Info/Debug
 ├─ PSRAM: "OPI PSRAM"                          ← ESP32-S3-CAM 有 PSRAM
 ├─ Arduino Runs On: "Core 1"                   ← 默认
 ├─ Events Run On: "Core 1"                     ← 默认
 ├─ 烧录器: "Esptool"                           ← 默认
 └─ ...
```

**关键配置详解：**

| 配置项 | 推荐值 | 说明 |
|--------|--------|------|
| **USB CDC On Boot** | `Enabled` | 必须启用，否则串口不可用 |
| **USB Mode** | `Hardware CDC and JTAG` | 启用硬件串口 |
| **Upload Speed** | `921600` 或 `115200` | 速度慢可降低 |
| **Flash Size** | `4MB` | 根据您的模块实际大小 |
| **PSRAM** | `OPI PSRAM` | ESP32-S3-CAM 带有 8MB PSRAM |
| **Partition Scheme** | `Default 4MB with spiffs` | 包含文件系统 |

---

## 5️⃣ 选择端口

**操作路径：** 工具 → 端口

**Windows 示例：**

```
工具
 └─ 端口 >
     ├─ COM1
     ├─ COM3 (USB-SERIAL CH340)                  ← 选择这个 ✓
     ├─ COM4
     └─ ...
```

**如何找到正确端口：**

1. **方法一：** 拔掉 USB 线，看哪个端口消失
2. **方法二：** 打开设备管理器：
   ```
   设备管理器
    └─ 端口 (COM 和 LPT)
        ├─ USB-SERIAL CH340 (COM3)              ← 这就是 ESP32
        └─ ...
   ```

**常见端口名称：**
- `USB-SERIAL CH340` - CH340 芯片
- `Silicon Labs CP210x` - CP2102 芯片
- `USB Serial Device` - 通用串口设备

---

## 6️⃣ 编译和上传按钮

**Arduino IDE 顶部工具栏：**

```
┌─────────────────────────────────────────────────────────┐
│  Arduino IDE                                            │
├─────────────────────────────────────────────────────────┤
│  文件  编辑  项目  工具  帮助                            │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [✓] [→] [⊡]  [串口监视器] [串口绘图仪]                │
│   ↑   ↑   ↑                                             │
│   │   │   └── 打开串口监视器                            │
│   │   └────── 上传（编译+烧录）                         │
│   └────────── 验证/编译                                 │
│                                                         │
│  esp32s3_ble_test.ino                              [×]  │
│  ┌─────────────────────────────────────────────────┐   │
│  │  #include <BLEDevice.h>                         │   │
│  │  ...                                            │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

**按钮说明：**
- **[✓] 验证/编译**：只编译，不上传（快捷键：Ctrl+R）
- **[→] 上传**：编译 + 烧录到板子（快捷键：Ctrl+U）
- **[⊡] 串口监视器**：查看串口输出（快捷键：Ctrl+Shift+M）

---

## 7️⃣ 串口监视器配置

**打开方式：** 工具 → 串口监视器（或 Ctrl+Shift+M）

**界面说明：**

```
┌─────────────────────────────────────────────────────────┐
│  COM3 上的串口监视器                              [×]   │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │  ==================================             │   │
│  │    ESP32-S3-CAM BLE 测试程序                   │   │
│  │    MiniAuto 小车项目                           │   │
│  │  ==================================             │   │
│  │                                                 │   │
│  │  [初始化] 正在初始化 BLE...                    │   │
│  │  [初始化] BLE 设备已启动！                     │   │
│  │  ...                                            │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  [                                            ] [发送]  │
│   ↑ 发送数据输入框                                      │
│                                                         │
│  [无行结束符 ▼]  [115200 波特 ▼]  [清除输出]  [时间戳] │
│                   ↑                                     │
│                   └── 设置为 115200                     │
└─────────────────────────────────────────────────────────┘
```

**关键设置：**
- **波特率**：选择 **115200**（右下角下拉菜单）
- **行结束符**：选择 **"无行结束符"** 或 **"换行符"**

---

## 8️⃣ 进入下载模式（硬件操作）

**ESP32-S3 开发板按钮位置：**

```
┌─────────────────────────────────────┐
│  ESP32-S3-CAM 开发板                │
│                                     │
│          [USB 接口]                 │
│             ║                       │
│        ┌────╨────┐                  │
│        │         │                  │
│   [BOOT]     [RST]  ← 两个按钮      │
│     ↑         ↑                     │
│     │         │                     │
│  IO0按钮   复位按钮                 │
│                                     │
│     [LED]                           │
│                                     │
│  [ESP32-S3 芯片]                    │
│                                     │
│  [摄像头模块]                       │
│                                     │
└─────────────────────────────────────┘
```

**进入下载模式步骤：**

1. **第一步**：按住 **BOOT** 按钮（不要松开）
2. **第二步**：按一下 **RST** 按钮（按下并松开）
3. **第三步**：松开 **BOOT** 按钮
4. **完成**：ESP32-S3 进入下载模式

**时序图：**

```
BOOT 按钮:  ━━━━┓                    ┏━━━━
                 ┗━━━━━━━━━━━━━━━━━━┛
                 按下      保持       松开

RST 按钮:   ━━━━━━━━┓  ┏━━━━━━━━━━━━━━━━
                     ┗━━┛
                     按  松

            ───────────────────────────────> 时间
                     ↑
                  进入下载模式
```

**指示灯说明：**
- 进入下载模式后，LED 可能会持续亮或灭
- 串口监视器会暂时无输出（正常现象）

---

## 9️⃣ 上传过程输出

**正常上传时的输出窗口（Arduino IDE 底部）：**

```
正在编译项目...
项目使用了 234567 字节，占用了 (57%) 程序存储空间。最大为 409600 字节。
全局变量使用了 12345 字节，(37%)的动态内存，余留 20579 字节局部变量。最大为 32924 字节。

esptool.py v4.5.1
Serial port COM3
Connecting....
Chip is ESP32-S3 (revision v0.1)
Features: WiFi, BLE
Crystal is 40MHz
MAC: 12:34:56:78:9a:bc
Uploading stub...
Running stub...
Stub running...
Changing baud rate to 921600
Changed.
Configuring flash size...
Flash will be erased from 0x00001000 to 0x00005fff...
Flash will be erased from 0x00008000 to 0x00008fff...
Flash will be erased from 0x0000e000 to 0x0000ffff...
Flash will be erased from 0x00010000 to 0x00041fff...
Compressed 17504 bytes to 12345...
Writing at 0x00001000... (100 %)
Wrote 17504 bytes (12345 compressed) at 0x00001000 in 0.5 seconds (effective 280.1 kbit/s)...
Hash of data verified.
Compressed 3072 bytes to 146...
Writing at 0x00008000... (100 %)
Wrote 3072 bytes (146 compressed) at 0x00008000 in 0.1 seconds (effective 246.2 kbit/s)...
Hash of data verified.
Compressed 8192 bytes to 47...
Writing at 0x0000e000... (100 %)
Wrote 8192 bytes (47 compressed) at 0x0000e000 in 0.1 seconds (effective 655.4 kbit/s)...
Hash of data verified.
Compressed 200752 bytes to 102345...
Writing at 0x00010000... (14 %)
Writing at 0x00018000... (28 %)
Writing at 0x00020000... (42 %)
Writing at 0x00028000... (57 %)
Writing at 0x00030000... (71 %)
Writing at 0x00038000... (85 %)
Writing at 0x00040000... (100 %)
Wrote 200752 bytes (102345 compressed) at 0x00010000 in 2.8 seconds (effective 573.4 kbit/s)...
Hash of data verified.

Leaving...
Hard resetting via RTS pin...
```

**成功标志：**
- 最后一行显示：`Hard resetting via RTS pin...`
- 没有错误信息（红色文字）

**常见错误输出：**

```
# 错误 1：未进入下载模式
esptool.py v4.5.1
Serial port COM3
Connecting........._____....._____...
A fatal error occurred: Failed to connect to ESP32: No serial data received.
上传错误: Uploading error: exit status 2

→ 解决：重新按 BOOT+RST 进入下载模式
```

```
# 错误 2：端口被占用
Serial port COM3
Connecting...
A fatal error occurred: Could not open COM3, the port is busy or doesn't exist

→ 解决：关闭串口监视器，重新上传
```

---

## 🔟 完整操作流程总结

**第一次烧录完整步骤：**

```
1. Arduino IDE → 文件 → 首选项
   └─ 添加 ESP32 板卡包网址

2. 工具 → 开发板 → 开发板管理器
   └─ 搜索 "esp32" → 安装

3. 文件 → 打开
   └─ 选择 esp32s3_ble_test.ino

4. 工具 → 开发板 → ESP32 Arduino
   └─ 选择 "ESP32S3 Dev Module"

5. 工具菜单
   └─ 配置所有参数（USB CDC、PSRAM 等）

6. 工具 → 端口
   └─ 选择 ESP32 对应的 COM 口

7. 点击 [✓] 编译
   └─ 确认编译成功

8. 硬件操作：进入下载模式
   └─ 按住 BOOT → 按 RST → 松开 BOOT

9. 点击 [→] 上传
   └─ 等待上传完成

10. 工具 → 串口监视器
    └─ 设置波特率 115200
    └─ 查看输出
```

---

## 📌 配置参数快速参考表

| 配置项 | 设置值 | 位置 |
|--------|--------|------|
| 开发板 | ESP32S3 Dev Module | 工具 → 开发板 |
| 端口 | COM3（根据实际） | 工具 → 端口 |
| USB CDC On Boot | Enabled | 工具 → USB CDC On Boot |
| USB Mode | Hardware CDC and JTAG | 工具 → USB Mode |
| Upload Speed | 921600 或 115200 | 工具 → Upload Speed |
| CPU Frequency | 240MHz (WiFi) | 工具 → CPU Frequency |
| Flash Size | 4MB (32Mb) | 工具 → Flash Size |
| Partition Scheme | Default 4MB with spiffs | 工具 → Partition Scheme |
| PSRAM | OPI PSRAM | 工具 → PSRAM |
| 串口波特率 | 115200 | 串口监视器右下角 |

---

## ⚠️ 常见错误配置

| 错误配置 | 后果 | 正确配置 |
|----------|------|----------|
| USB CDC On Boot: Disabled | 串口无输出 | Enabled |
| 波特率选错（如 9600） | 串口乱码 | 115200 |
| 开发板选错（如 ESP32） | 编译错误 | ESP32S3 Dev Module |
| 未进入下载模式 | 烧录失败 | 按 BOOT+RST |

---

**如有疑问，请参考完整的 README.md 文档或联系技术支持。**
